"""numstats -- a tiny statistics CLI used as a GitLab CI/CD starter project.

The domain logic here is deliberately trivial. The point of the project is the
*pipeline* around it, not the maths. When you are ready to grow it, swap the
functions in ``core.py`` for real domain logic (for example TLE parsing).
"""

__version__ = "0.1.0"
